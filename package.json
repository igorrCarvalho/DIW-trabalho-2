{
  "name": "videotube",
  "version": "1.0.0",
  "description": "VideoTube - Trabalho Prático 2 de DIW",
  "scripts": {
    "start": "json-server --watch db/db.json --static ./public"
  },
  "devDependencies": {
    "json-server": "0.17.4"
  }
}
